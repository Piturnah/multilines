# Multiline Formatting Module
Python module for formatting multi-line strings

## Getting started

```bash
pip install multilines-piturnah
```

```python
from mlpkg import multilines as ml
ml.help()
```
